# Fork Presets

Store fork-specific YAMLs, startup presets, and environment examples here.

Suggested usage:

- Keep upstream-compatible examples out of `src/`.
- Add team presets and TUI startup YAML files in this directory.
- Reference these files from your own docs and scripts instead of editing upstream examples in place.
